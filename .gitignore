# Node / pnpm
node_modules/

# Environments
.env

# Editors
.idea
.vscode

# macOS
.DS_Store

# Generated output (result.pdf plus the per-template result-<variant>.pdf)
result*.pdf

# Local private configs (hold your real PII - never commit).
# Covers config-private.yaml and every per-template config-private-<variant>.yaml.
config-private*.yaml
temp.md
